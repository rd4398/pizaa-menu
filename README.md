## A simple pizza-menu app using typescript

### Dependencies

- parcel.js
- json-server

### How to run the app
- Install all dependencies from `package.json`
- `$npx parcel src/*.html` to bundle the code
- `$json-server --watch db.json` run the json server in seperate terminal
